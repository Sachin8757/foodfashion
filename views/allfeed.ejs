<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/allfeedback.css">
    <link rel="stylesheet" href="/stylesheets/display.css">
    <title>Document</title>
</head>
<body>
    <div class="box background">
        <div class="contaner">
            <% feedback.forEach(function(feedback){ %>
                <div class="box2">
                <h3 class="data">Name:- <%=feedback.name%></h3>
                <h3 class="data">Phone No:- <%=feedback.phone%></h3>
                <hr>
                <p class="feed"> <%=feedback.feed%></p>
            </div>
           <% })%>
        </div>
    </div>
</body>
</html>